FROM nginx:1.27.3-alpine
COPY ./nginx/cabestan.conf.model /cabestan.conf.template
CMD ["/bin/sh" , "-c" , "envsubst '$DJANGO_HTTP_PORT' < /cabestan.conf.template > /etc/nginx/conf.d/cabestan.conf && exec nginx -g 'daemon off;'"]