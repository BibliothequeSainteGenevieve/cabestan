FROM nginx:1.27.3-alpine
COPY ./cabestan.conf.model /cabestan.conf.template
CMD ["/bin/sh" , "-c" , "envsubst < /cabestan.conf.template > /etc/nginx/conf.d/cabestan.conf && exec nginx -g 'daemon off;'"]